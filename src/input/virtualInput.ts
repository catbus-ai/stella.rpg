// Shared virtual input state written by TouchControls, read by Stella.
// Plain mutable object — no events needed.
export const virtualInput = {
  up:    false,
  down:  false,
  left:  false,
  right: false,
}
